"""FastAPI surface: proxy routes, admin routes, health.

Phase 0 ships only ``/healthz`` (see :mod:`headroom.api.main`). The dialect proxy
routes land in Phase 1 and the admin routes in Phase 2, as additions.
"""
